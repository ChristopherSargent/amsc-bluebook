# Local state (never commit)
*.tfstate
*.tfstate.backup
.terraform/
.terraform.lock.hcl

# Plan files contain sensitive data
*.tfplan

# Var files with secrets
*.auto.tfvars
override.tf
override.tf.json
